import * as React from 'react';

import { IActionButton } from '@/ActionButton';
import { ActionButton as Layout } from './Layout';

export const ActionButton = (props: IActionButton) => <Layout {...props} />;
